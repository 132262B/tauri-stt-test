[package]
name = "stt-sidecar-proto"
version = "0.1.0"
edition = "2021"
description = "Rust↔Python 사이드카 메시지/프레임 스키마 (stt_mlx 와 동일 계약, docs/02-architecture.md C.2)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
